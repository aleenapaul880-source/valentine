<!DOCTYPE html>
<html>
<head>
<title>For Aryan ❤️</title>

<style>
body {
    text-align: center;
    font-family: "Comic Sans MS", cursive;
    background: linear-gradient(to bottom right, #ffe6f0, #ffd6e7);
    overflow: hidden;
}

h1 {
    color: #ff4d88;
}

button {
    padding: 15px 30px;
    font-size: 18px;
    margin: 15px;
    border: none;
    border-radius: 15px;
    cursor: pointer;
}

#yesBtn { background-color: #ff4d88; color: white; }
#noBtn { background-color: #cccccc; }

.heart {
    position: absolute;
    font-size: 30px;
    cursor: pointer;
    animation: fall 3s linear forwards;
}

@keyframes fall {
    from { top: -50px; }
    to { top: 100vh; }
}
</style>
</head>

<body>

<audio autoplay loop>
    <source src="https://files.catbox.moe/7r6l2o.mp3" type="audio/mp3">
</audio>

<div id="gameArea">
    <h1>Aryan ❤️ Catch 10 hearts to unlock something special 🥺</h1>
    <h2>Score: <span id="score">0</span></h2>
</div>

<div id="questionArea" style="display:none;">
    <h1>Aryan 🥺❤️</h1>
    <p>You make Esha smile way too much...</p>
    <h1>Will you be Esha’s Valentine? 💖</h1>

    <button id="yesBtn" onclick="yesClicked()">YES OF COURSE 😍</button>
    <button id="noBtn" onclick="moveButton()">No 😒</button>
</div>

<script>
let score = 0;

/* Create falling hearts */
function createHeart() {
    let heart = document.createElement("div");
    heart.innerHTML = "❤️";
    heart.classList.add("heart");
    heart.style.left = Math.random() * 100 + "vw";

    heart.onclick = () => {
        score++;
        document.getElementById("score").innerText = score;
        heart.remove();

        if(score >= 10) {
            document.getElementById("gameArea").style.display = "none";
            document.getElementById("questionArea").style.display = "block";
        }
    };

    document.body.appendChild(heart);

    setTimeout(() => heart.remove(), 3000);
}

setInterval(createHeart, 800);

/* YES message */
function yesClicked() {
    document.body.innerHTML = `
        <h1>YAYYYY ARYAN ❤️</h1>
        <p>You just made Esha the happiest girl 🥺💖</p>
        <p>Get ready for cuddles, love, and lots of annoying attention 😌</p>
        <p>I love youuu 💕</p>
    `;
}

/* Escaping NO button */
function moveButton() {
    var button = document.getElementById("noBtn");
    var x = Math.random() * (window.innerWidth - 100);
    var y = Math.random() * (window.innerHeight - 50);
    button.style.position = "absolute";
    button.style.left = x + "px";
    button.style.top = y + "px";
}
</script>

</body>
</html>
